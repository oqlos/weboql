# SCENARIO: Auto-generated API Smoke Tests
# TYPE: api
# GENERATED: true
# DETECTORS: TestEndpointDetector

CONFIG[4]{key, value}:
  base_url, http://localhost:8101
  timeout_ms, 10000
  retry_count, 3
  detected_frameworks, TestEndpointDetector

# REST API Endpoints (1 unique)
API[1]{method, endpoint, expected_status}:
  GET, /api/v1/schema, 200

ASSERT[2]{field, operator, expected}:
  status, <, 500
  response_time, <, 2000

# Summary by Framework:
#   inferred-from-tests: 1 endpoints